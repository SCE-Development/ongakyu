# WebSocket URL of the Oracle backend (must match PI_WS_PATH on the server)
ORACLE_WS_URL=wss://your-oracle-host/ws/pi

# Shared secret used to authenticate with the Oracle bridge
PI_BRIDGE_SECRET=change-me

# Optional overrides
# MPV_SOCKET=/tmp/mpv-socket
# MPV_BIN=mpv
# YTDLP_BIN=yt-dlp
